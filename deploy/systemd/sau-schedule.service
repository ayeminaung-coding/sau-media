[Unit]
Description=Release due scheduled social posts
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/curl --fail --silent --show-error -X POST http://127.0.0.1:8001/schedule/tick
