# Where the FastAPI control plane is reachable *from the browser*.
# Baked in at build time; the console's Settings field overrides it at runtime
# and remembers the override in localStorage.
VITE_API_BASE_URL=http://localhost:8001
